# HANDOFF — 2026-08-08 · local norm + SIR + Q1 다층 탐지 체계 확립

이 문서는 **세션 경계 상태 + 연구 일지**다. 프로젝트 개요는 `README.md`.
v1 프로젝트(`E:\game\Music Hermeneutic AI\`)와는 독립된 후속 프로젝트.

**작업 이력의 전문은 아카이브에 있다** — `Docs/Archive/handoff/`.

---

## 0. 한 문단으로 — 지금 어디인가

**3종 탐지 기법이 각각 유효성을 입증, 트랙별 최적 조합이 다름을 확인했다.**
전역 Otsu 1차 탐지가 SS에서 우수, Q1 rescue가 GL 피아노에서 우수,
local norm(16s)이 VN 밀집 구간에서 압도적. SIR(u3, 분광 균일도)은
cry 과다탐지 억제에 유용하나 Q1과 병용 필요.
다음 단계: 사용자의 새 기법 제안을 듣고 pipeline 통합 방향 결정.

---

## 1. 세션 이력

### 세션 1 (2026-08-07~08) — 파이프라인 구축 + Q1 발견

v1 코드 이식 → 파이프라인 → 맛보기 4응답 → 피아노 과소탐지 진단 → 6종 보정 시도
→ Q1 프로토타입 rescue 유효 확인 (GL 0-4s: 12→19, 인간 18).
mid-band rescue 오탐 판정. HANDOFF 생성, GitHub push 완료.

### 세션 2 (2026-08-08) — 청취 검증 + 천체 기법 응용 + local norm 돌파

#### 2-a. morphological opening — 실패

성운의 별-성운 분리(grey_opening)를 음향에 적용:
- opening 단독: GL 16-20s 6→4 (수치 일치), GL 0-4s 12→9 (악화)
- Q1→opening 필터: **filtered=0** — 하드코어에서 모든 사건이 충격적

**결론: opening은 하드코어 음악에 무효.** 스템 전량 삭제.

#### 2-b. SIR (Spectral Identity Rescue) — 부분 유효

Fable에 의뢰. 성운 파이프라인의 `uniformity = ch_min/ch_max`를 음향에 반전 적용:
- 천체: 별=백색=노이즈, 성운=유색=신호
- 음향: 참 사건=다대역 동시 변화("백색"), 비사건=단일 대역("유색")

**u2 분리도**: base 중앙값 0.36~0.50 vs sub 0.000 — 4트랙 전부에서 물리적 가설 성립.
**SIR(u3)**: cry 과다탐지 18→14 억제, GL 16-20s 9→10으로 Q1보다 인간(14)에 근접.
**판정**: Q1과 병용해야 함. 단독으로는 VN 200-204에서 Q1(6)에 열세(4).

#### 2-c. 전 트랙 청취 검증 — 핵심 관측

전체 길이 소니파이 생성 후 사용자 청취:

| 관측 | 내용 |
|------|------|
| Q1전체_클릭 | 가장 안정적 음원 (사용자 판정) |
| cry 0-4s | Q1 과다탐지 (조용 구간 프로토타입 불안정) |
| cry 링잉 | 잔향에서 2회 찍힘 (급한 이슈 아님) |
| GL 16-20s | 사용자 인지 신스 킥 4회를 어떤 방법도 정확히 못 캐치 |
| 연속 신스 비트 | Q1이 많이 찍은 구간에서 오히려 잘 잡아냄 (긍정) |
| VN 3:00대 | 음으로 가득한 밀집 구간에서 모든 방법이 0 탐지 |
| SIR 일부 음 누락 | sir_클릭에서 있어야 할 음이 사라진 케이스 관측 |

#### 2-d. VN 밀집 구간 진단 — 다이내믹 레인지 문제

VN 176-240s (3분대): 극대점 2333개 중 Otsu 초과 단 20개 (0.9%).
전역 Otsu=2.82인데 구간 env_max=1~3. 인트로(22)와 킥(15)이 임계를 지배.

**성운 파이프라인 대응**: BASELINE.md STEP 2 — "전역 99-pct는 Ha 절대 밝기가
OIII 픽셀을 임계 아래로 밀어냄 → 각 채널 자기 99-pct 정규화 후 union으로 수정."

#### 2-e. asinh 압축 시도 — 실패

process_nebula_v2.py의 asinh stretch 적용 → Otsu가 함께 올라가 오히려 악화.
SuperFlux(스펙트럼 변화)는 천문 이미지 밝기와 다른 분포.

#### 2-f. local norm(16s) — 돌파

성운 STEP 2 "채널별 자기 99-pct 정규화"를 시간 축에 적용:
- 16초 블록별 양수값의 99-percentile로 포락선 정규화
- 정규화 후 전역 Otsu → 조용한 구간의 사건이 시끄러운 구간과 같은 스케일에서 경쟁

**VN 밀집 구간**: Otsu 초과 0.9% → **25.9%**. 3분대에서 0→12~23개 탐지.

#### 2-g. 전체 길이 청취 검증 — 최종 판정

| 트랙 | 최우수 방법 | 판정 근거 |
|------|-------------|-----------|
| **VN** | **norm** | 3분대 밀집 구간이 압도적으로 우수 |
| **SS** | **1차 탐지(Otsu)** | norm/Q1/SIR 모두 기존보다 열세 |
| **GL** | Q1 rescue | 피아노 구간 12→19 (인간 18) |
| **cry** | 추가 검증 필요 | Q1 과다, SIR이 억제에 유용 |

**핵심 결론: 단일 방법 만능은 없다. 트랙/구간 특성에 따라 최적 조합이 다름.**

---

## 2. **다음 세션은 여기부터 읽는다**

### 현재 상태

- **3종 기법 유효성 확인**: 1차 Otsu, Q1 rescue, local norm, SIR(보조)
- **pipeline 미통합**: 모든 기법이 진단 스크립트에만 존재
- **설문 클립**: peaks_with_mid 기준 (교체 필요)
- **소니파이**: `out/sonify/{트랙}/` — 전체 길이 6종 + 구간별 4종
- **사용자**: 새 기법 추가 제안 예정

### 소니파이 파일 현황

```
out/sonify/{트랙}/
  전체_1차탐지_클릭.wav    — Otsu base (3kHz)
  전체_Q1_클릭.wav         — Q1 (base 3kHz + 구조 5kHz)
  전체_SIR_클릭.wav        — SIR(u3) (base 3kHz + 구조 5kHz)
  전체_norm_클릭.wav       — norm(16s) base (3kHz)
  전체_norm+Q1_클릭.wav    — norm + Q1 rescue (3kHz/5kHz)
  전체_norm+SIR_클릭.wav   — norm + SIR rescue (3kHz/5kHz)
  {구간}/
    원곡.wav, 1차탐지_클릭.wav, Q1전체_클릭.wav, Q1구조만_클릭.wav,
    SIR_클릭.wav, SIR_구조만_클릭.wav
```

### 다음 단계

1. **사용자의 새 기법 제안 청취**
2. **최종 탐지 방법 확정** → pipeline 통합 전략 결정
3. 파이프라인 재실행 → 설문 재생성 → 48개 응답 수집

### 열려 있는 문제

- 단일 방법 만능 없음 — 조합 전략 미확정
- peaks_with_mid 교체 필요 (pipeline.py)
- cry 0-4s Q1 과다탐지
- cry 링잉 이중 탐지
- GL 16-20s 신스 킥 4회 미캐치
- survey_gen.py append 버그
- 분류 모듈(classify.py) — 총 계수 검증 통과 후

---

## 3. 탐지 기법 인벤토리

### 확정 유효

| 기법 | 원리 | 강점 | 약점 | D-21 |
|------|------|------|------|------|
| **Otsu 1차** | 전역 Otsu 이진 임계 | SS 최우수, 안정적 | 약한 사건 과소탐지 | 0 파라미터 |
| **Q1 rescue** | 윈도우 내 대역 프로토타입 유사도 Q1 | GL 피아노 12→19 | cry 과다, base<2 시 무력 | 0 파라미터 |
| **local norm(16s)** | 블록별 99-pct 자기 정규화 후 Otsu | VN 밀집 구간 압도적 | SS에서 기존보다 열세 | 블록크기=16s (프레이즈 단위) |

### 보조 유효

| 기법 | 원리 | 역할 |
|------|------|------|
| **SIR(u3)** | 분광 균일도 ch_min/ch_max + 2-of-3 동시극대 | cry 과다탐지 억제, Q1 병용 |

### 기각

| 기법 | 사유 |
|------|------|
| mid-band rescue | 사용자 오탐 판정 |
| morphological opening | 하드코어 전 사건 충격적, filtered=0 |
| asinh 압축 | Otsu가 함께 상승, 역효과 |
| local Otsu | VN 폭발 |

---

## 4. 파이프라인

```
오디오 → LUFS 정규화 → STFT → mel → SuperFlux 온셋 포락선
                                          ↓
                            [탐지 기법 조합 — 미확정]
                                          ↓
                            4초 윈도우 계수 → 블라인드 설문
```

### Q1 프로토타입 rescue 원리

1. 1차 탐지 피크(Otsu 초과)의 대역 프로파일 [low, mid, high] 추출
2. 프로파일 중앙값 = 원형(prototype)
3. 탐지 피크 ↔ 원형 간 코사인 유사도의 Q1(25%) = 구조 하한
4. Otsu 미달 극대점 중 원형 유사도 ≥ Q1인 것을 구조
5. 전체 후보 진폭 내림차순 탐욕 선택 + 30ms 최소간격

### local norm(16s) 원리

성운 파이프라인 STEP 2 "각 채널 자기 99-pct 정규화"의 시간 축 응용:
1. 포락선을 16초 블록으로 분할
2. 각 블록의 양수값을 자기 99-percentile로 나눔 (0~1 스케일)
3. 정규화된 포락선에 전역 Otsu
4. 결과: 조용한 구간의 사건이 시끄러운 구간과 같은 스케일에서 경쟁

### SIR(u3) 원리

성운 파이프라인의 분광 균일도(ch_min/ch_max)를 음향에 반전 적용:
- 참 사건 = 다대역 동시 스펙트럼 변화 ("백색" 온셋) → uniformity 높음
- 비사건 = 단일 대역 변동 ("유색") → uniformity 낮음
- Otsu 미달 극대점 중 u3 ≥ Otsu(극대점 u3 분포) ∧ 2-of-3 대역 동시극대 → 구조

---

## 5. 코드 상태

| 파일 | 상태 | 비고 |
|------|------|------|
| `src/config.py` | 안정 | |
| `src/audio_io.py` | 안정 | |
| `src/onset.py` | 안정 | SuperFlux + 3밴드 |
| `src/peak_pick.py` | **교체 필요** | peaks_with_mid → 조합 탐지 |
| `src/pipeline.py` | **교체 필요** | |
| `src/sonify.py` | 안정 | |
| 기타 (counter, survey_gen 등) | 안정 | |

### 진단 스크립트

| 파일 | 판정 |
|------|------|
| `_diag_proto_q1.py` | **Q1 — 유효** |
| `_diag_spectral_identity.py` | **SIR — 보조 유효** |
| `_diag_local_norm.py` | **local norm — 유효** |
| `_diag_asinh.py` | asinh — 실패 |
| `_diag_vn_dense.py` | VN 밀집 구간 진단 |
| `_diag_morpho*.py` | opening — 기각 |
| `_diag_q1_then_filter.py` | Q1→opening 필터 — 기각 |
| `_sonify_q1_all.py` | 구간별 Q1 소니파이 |
| `_sonify_sir.py` | 구간별 SIR 소니파이 |
| `_sonify_full.py` | 전체 길이 Q1/SIR 소니파이 |
| `_sonify_localnorm.py` | 전체 길이 norm 소니파이 |
| 기타 `_diag_*`, `_sonify_*` | 세션 1 유물 |

---

## 6. 규율

| ID | 규율 |
|---|---|
| D-07 | 에너지 계열 금지 — SuperFlux(스펙트럼 변화) 사용 |
| D-18 | 천장(r~0.85)과 바닥(r~0) 사전 정의 |
| D-21 | 출력 보고 파라미터 고르지 않는다 |
| D-24 | 관측과 해석을 분리 |
| D-v2-01 | 설문 완료 전 ground_truth.json 열지 않음 |
| D-v2-03 | 윈도우 4초 고정 |

---

## 7. 환경

```
Python 3.14.2  (C:\Python314)
.venv          v1과 공유 (E:\game\Music Hermeneutic AI\.venv)
GPU            RTX 3080 10 GB
```

콘솔 cp932 — 한글 출력은 `reconfigure(encoding="utf-8")`.
오디오: v2/audio/ 우선, v1 `audio/target/` 폴백.
대상 4곡: cry of viyella, Grievous Lady, Viyella's Nightmare, Swift Swing.

---

## 8. 맛보기 응답 + 기법별 수치 (참고용)

| 구간 | Otsu | Q1 | SIR(u3) | norm | norm+Q1 | 인간 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| GL 0-4 | 12 | **19** | 15 | 12 | 12 | **18** |
| GL 16-20 | 6 | 9 | 10 | **10** | — | **14** |
| VN 200-204 | 2 | **6** | 4 | 11 | — | **6** |
| VN 268-272 | 12 | 12 | 12 | **13** | — | **14** |
| VN 3:00-3:04 | 0 | 0 | 0 | **19** | — | ? |
| cry 0-4 | 3 | 18(과다) | **14** | 3 | — | ? |

### 전체 트랙 피크 수

| 트랙 | Otsu | Q1 | SIR | norm | norm+Q1 | norm+SIR |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| cry | 564 | 766 | 610 | 595 | 795 | 590 |
| GL | 591 | 638 | 629 | 582 | 648 | 516 |
| VN | 264 | 454 | 401 | **908** | **1193** | **920** |
| SS | **558** | 693 | 587 | 502 | 617 | 473 |

**청취 판정**: VN=norm 압도적, SS=1차탐지 최우수.
