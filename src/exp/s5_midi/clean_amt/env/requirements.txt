# clean_amt env — independent of s4_piano runtimes
# Base: Python 3.11 venv under env/.venv
mido>=1.3.0
PyYAML>=6.0
soundfile>=0.12
numpy>=1.24
# Pilot A — on Windows install ncls with a binary wheel first if needed:
#   pip install ncls --only-binary=:all:
# Torch stack (pin matching CUDA builds; do NOT let pip pull mismatched torchaudio):
#   pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#   pip install transkun==2.0.1
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
ncls>=0.0.68
transkun==2.0.1
# Audit B (install when running M2; optional)
# basic-pitch==0.4.0
